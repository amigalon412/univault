# Dependencies
node_modules/
keeper/node_modules/
.pnp
.pnp.js

# Build
.next/
out/
build/
dist/
*.tsbuildinfo

# Foundry build output. `lib/` itself is NOT ignored: the dependencies live
# there as pinned submodules, which is what makes the build reproducible.
contracts/out/
contracts/cache/
contracts/broadcast/

# Environment
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# TypeScript
next-env.d.ts

# Vercel
.vercel

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Local settings (not project config)
.claude/settings.local.json

# User-specific skills (not part of the template)
.claude/skills/customize/
.claude/skills/release/

# Local temp files
temp/

# Runtime settings written by /admin (the published $BLUR address). On a server
# this lives in BLUR_DATA_DIR; ./.data is only the local development default.
.data/

# Playwright MCP scratch output
.playwright-mcp/
BlurVault/app/
